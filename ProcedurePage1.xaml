<Page x:Class="health.ProcedurePage1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:health"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProcedurePage1">

    <Grid>
        <DataGrid Name="ProcedureTable" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Width="*" Binding="{Binding title}"/>
                <DataGridTextColumn Header="Описание" Width="3*" Binding="{Binding description}"/>
                <DataGridTextColumn Header="Стоимость" Width="*" Binding="{Binding price}"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
